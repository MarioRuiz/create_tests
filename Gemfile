source 'https://rubygems.org'

group :test do
  gem 'rake'
  gem 'rspec'
  # Needed to load generated settings/tests (smoke checks and local example runs)
  gem 'nice_http', '~> 1.10'
end

# Specify your gem's dependencies in mygem.gemspec
gemspec